export type FOLDER = {
  _id: string;
  parent_id: string;
  name: string;
  total_files: string;
  total_folders: string;
  document_type: string;
}

export type MAIN = {
  id: string;
  name: string;
}